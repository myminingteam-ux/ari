const mandateContent={strategy:{index:'01 / 03',title:'Strategic advisory',copy:'Frame the decision, expose its commercial implications, and establish a route that can be executed with confidence.',items:['Positioning and strategic direction','Decision architecture and stakeholder alignment','Commercial narrative and executive preparation']},capital:{index:'02 / 03',title:'Capital & growth',copy:'Clarify the growth thesis, sharpen the opportunity, and align the capital story with the decision makers who matter.',items:['Growth strategy and investment narrative','Capital partner positioning','Executive preparation and stakeholder alignment']},partnerships:{index:'03 / 03',title:'Partnerships & market entry',copy:'Translate strategic ambition into qualified relationships, focused market access, and an executable path into new territory.',items:['Market-entry strategy','Partner identification and qualification','Executive introductions and activation']}};const panel=document.querySelector('#mandate-panel');document.querySelectorAll('.mandate-tab').forEach(tab=>tab.addEventListener('click',()=>{document.querySelectorAll('.mandate-tab').forEach(t=>{t.classList.remove('active');t.setAttribute('aria-selected','false')});tab.classList.add('active');tab.setAttribute('aria-selected','true');const item=mandateContent[tab.dataset.panel];panel.innerHTML=`<p class="panel-index">${item.index}</p><h3>${item.title}</h3><p>${item.copy}</p><ul>${item.items.map(i=>`<li>${i}</li>`).join('')}</ul><a class="text-link" href="#contact">View mandate</a>`}));const processCopy={Diagnose:'Clarify the decision, constraints, stakeholders and commercial stakes.',Position:'Define the strategic frame, commercial narrative and route forward.',Connect:'Identify and activate the relationships that can materially advance the mandate.',Execute:'Turn alignment and access into disciplined, accountable action.'};document.querySelectorAll('.process-step').forEach(step=>step.addEventListener('click',()=>{document.querySelectorAll('.process-step').forEach(s=>s.classList.remove('active'));step.classList.add('active');document.querySelector('#process-detail').textContent=processCopy[step.dataset.process]}));const menuButton=document.querySelector('.menu-button');const mobileMenu=document.querySelector('#mobile-menu');menuButton.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')==='true';menuButton.setAttribute('aria-expanded',String(!open));mobileMenu.hidden=open});mobileMenu.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{mobileMenu.hidden=true;menuButton.setAttribute('aria-expanded','false')}));const dialog=document.querySelector('.engagement-dialog');document.querySelector('[data-dialog-open]').addEventListener('click',()=>dialog.showModal());document.querySelector('.dialog-close').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',event=>{const box=dialog.getBoundingClientRect();if(event.clientX<box.left||event.clientX>box.right||event.clientY<box.top||event.clientY>box.bottom)dialog.close()});