const mandateContent={strategy:{index:'01 / 03',title:'Strategic advisory',copy:'We help leadership teams define direction, solve complex challenges and build strategies that create enduring competitive advantage.',items:['Strategy and positioning','Commercial due diligence','Operating model and performance']},capital:{index:'02 / 03',title:'Capital & growth',copy:'We sharpen the growth thesis, strengthen the investment narrative and align the opportunity with the capital partners who matter.',items:['Growth strategy and investment narrative','Capital partner positioning','Executive preparation and alignment']},partnerships:{index:'03 / 03',title:'Partnerships & market entry',copy:'We translate strategic ambition into qualified relationships, focused market access and an executable path into new territory.',items:['Market-entry strategy','Partner identification and qualification','Executive introductions and activation']}};
const panel=document.querySelector('#mandate-panel');
document.querySelectorAll('.mandate-tab').forEach(tab=>tab.addEventListener('click',()=>{document.querySelectorAll('.mandate-tab').forEach(item=>{item.classList.remove('active');item.setAttribute('aria-selected','false')});tab.classList.add('active');tab.setAttribute('aria-selected','true');const item=mandateContent[tab.dataset.panel];panel.innerHTML=`<p class="panel-count">${item.index}</p><h3>${item.title}</h3><p>${item.copy}</p><ul>${item.items.map(entry=>`<li>${entry}</li>`).join('')}</ul><button class="outline-button" type="button" data-contact-open>Explore mandate</button>`;panel.querySelector('[data-contact-open]').addEventListener('click',openContact)}));
const processCopy={Diagnose:'Understand the mandate, define the challenge and establish what success must look like.',Position:'Build the right strategic angle, commercial narrative and route forward.',Connect:'Introduce the right stakeholders, qualify relationships and open the doors that matter.',Execute:'Support delivery, maintain momentum and drive measurable results.'};
document.querySelectorAll('.process-step').forEach(step=>step.addEventListener('click',()=>{document.querySelectorAll('.process-step').forEach(item=>item.classList.remove('active'));step.classList.add('active');document.querySelector('#process-detail').textContent=processCopy[step.dataset.process]}));
const menuButton=document.querySelector('.menu-button');const mobileMenu=document.querySelector('#mobile-menu');
menuButton.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')==='true';menuButton.setAttribute('aria-expanded',String(!open));mobileMenu.hidden=open});
mobileMenu.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{mobileMenu.hidden=true;menuButton.setAttribute('aria-expanded','false')}));
const engagementDialog=document.querySelector('.engagement-dialog');document.querySelector('[data-dialog-open]').addEventListener('click',()=>engagementDialog.showModal());engagementDialog.querySelector('.dialog-close').addEventListener('click',()=>engagementDialog.close());
const contactDialog=document.querySelector('.contact-dialog');function openContact(){contactDialog.showModal();mobileMenu.hidden=true;menuButton.setAttribute('aria-expanded','false')}document.querySelectorAll('[data-contact-open]').forEach(button=>button.addEventListener('click',openContact));contactDialog.querySelector('.contact-close').addEventListener('click',()=>contactDialog.close());
[engagementDialog,contactDialog].forEach(dialog=>dialog.addEventListener('click',event=>{const box=dialog.getBoundingClientRect();if(event.clientX<box.left||event.clientX>box.right||event.clientY<box.top||event.clientY>box.bottom)dialog.close()}));